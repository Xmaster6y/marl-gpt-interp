defaults:
  - optional grf_rollout_stats: null
  - optional env_mechanism_probes: null
  - optional cross_env_compute_sharing: null
  - optional normalize_soccer_data: null
  - optional compare_soccer_stats: null
  - _self_

hydra:
  run:
    dir: results/hydra/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}
  sweep:
    dir: results/hydra/${hydra.job.name}_sweep/${now:%Y-%m-%d_%H-%M-%S}

wandb:
  project: marl-gpt-interp
  entity: yp-edu
  mode: offline
